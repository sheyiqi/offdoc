#### CEN-Gating

[Company Name] memory compilers optionally support the Chip Enable Gating feature.

By default, the address `A` pins  is not gated by the `CEN` pin may increase toggle power. In this case, `CEN` requires a relatively small setup time.

To reduce the toggle power of the address `A` pins, users can enable the `CEN-Gating feature`; however, the setup time of CEN may increase.

