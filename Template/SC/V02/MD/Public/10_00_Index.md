## Index
`A` Address 37  
`ATPG` 64  
`B` Back end views 69  
Best operating point 60  
`BIST`. Built-In Self Test (BIST) 17  
bitmap file 71  
Block diagram 34  
Built-In Self Test (BIST) 17  
`C` check.tcl 57  
Column Mux range 37  
Compiler directories, using 57  
Compiler directory structure 57  
Compiler features 16  
compiler library files 57  
`.cir` 57  
`.del` 58  
`.gds` 58  
`.glb` 58  
`.pf` 58  
`.ppr` 58  
`.prm` 58  
`.raw` 58  
`.v` 58  
custom.glb 57  
compout/views directory 60  
compout/views output files `_allpvt.ds` 60  
`_coord.txt` 60  
`_func.v` 60, 61  
`_hcell.txt` 61  
`_rtl.vhd` 61  
`_stim.v` 61  
`_verilog_vhd_stim.v` 61  
`.cfg` 60  
`.cir` 60  
`.db` 60  
`.gds` 60  
`.lib` 61  
`.log` 60  
`.plef` 60  
`.summ` 61  
`.v` 61  
compout/views/instance_name/Best(or)Typical(or)Worst content 61  
cover_report 59  
cover.sts 59  
ctmc_report 60  
Custom global file (`custom.glb`) config 76  
setting parameters 76  
`D` Directory structure 57  
dual rail 17  
`E` Extended Layer Translation Table (`XLTT`) file 70  
Extended Layer Translation Table file configuring 77  
`G` GDSII file 70  
GDSII library, naming 70  
gdsview.cfg 57  
Generating pins 77  
`I` Input pin. Output pin 21  
Instance directory 60  
`L` Layout Exchange Format models 69  
`M` Memory 40  
MemoryBIST 66  
`N` Naming GDSII libraries 70  
`O` output files 59  
`P` pg_enable 16  
Pins, generating 77  
power gating 16  
`R` Read-only files 57  
release.txt 57  
Revision history 3  
`S` scramble information 71  
Self Time bypass 19  
Setting custom.glb file parameters 76  
Specifications 26  
SPICE netlist 70  
Subword or Bit Write 21  
`T` Table 77  
Typical operating point 60  
`U` User-editable files 57  
`V` vdda_enable 17  
views/ 59  
`W` WE. Write-Enable Mask (WEM) pin  
Word 37  
Worst operating point 60  
Write 21  
Write-Enable Mask (WEM) pin 21  
`X` xltt 57
