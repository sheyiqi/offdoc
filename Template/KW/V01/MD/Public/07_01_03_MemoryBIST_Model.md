#### MemoryBIST Models

[Company Name] memory compiler provides both BIST and scan solutions to verify memory. The time taken to scan in inputs and scan out results increases the total test time. The BIST models generate synthesis scripts for the BIST controller and test cases based on standard algorithms.
[Company Name] memory compiler supports the STAR Memory System (`<instance_name>.masis`) and Tessent (`<instance_name>.memlib`). 