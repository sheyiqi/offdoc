### Using Compiler Library Files

The files that make up the compiler are located under the `<compiler>` directory, in `/ip/process_name/ip_name/version_name`. These files include the compiler parameter definition files, compiler characterization data, netlists and layout information of the leaf cells used to build memory instances, View Templates, and other related files.