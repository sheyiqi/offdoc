#### Low Power Formats

[Company Name] memory compiler supports `Low Power Formats` and provide two views: `UPF - "<instance_name.upf>"` and `CPF - "<instance_name.cpf>"`. These views are used to describe the power intent of memory instances.

The views include power domains, power state transitions, and isolations.