#### IP Tagging

[Company Name] labels its `Intellectual Property (IP)` with either the use of the `IP marking layer` or a `VSIA tag layer`. This `GDSII layer` identification is required to correctly label SpreadT's IP at the foundry.
It is imperative that customers include this layer, in the form `layer:datatype`, when ordering masks.

