**[Company Name] Product Release**

[Company Simplify Name] regularly releases new pre-silicon product versions (version V). To address high-priority change requests, [Company Simplify Name] also performs patch releases from time to time. Patch releases use the suffix `pn`, where `n` is the release number, such as 1, 2 or 3 (`p1`, `p2` or `p3`).

Refer to the release.txt file in the complib directory for a list of release dates.

