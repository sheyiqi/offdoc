#### ATPG Models

[Company Name] memory compiler supports the ATPG tools `TetraMAX` and `FastScan`.
The `Tetramax` (`<instance_name>.tmax`) and `Fastscan` (`<instance_name>.fastscan`) models contain the memory core description.